{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Deploy Eleventy Site",
      "type": "shell",
      "command": "npx @11ty/eleventy && git push",
      "group": "build",
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
